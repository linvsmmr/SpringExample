<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.eunbi.springexample.mvc.UserRepository">
    <select id="selectLastUser"
            resultType="com.eunbi.springexample.mvc.User">
        SELECT
            `id`
        ,`name`
        ,`yyyymmdd`
        ,`email`
        ,`introduce`
        ,`created_at`
        ,`updated_at`
        FROM
            `new_user`
        ORDER BY
            `id` DESC
        LIMIT 1
    </select>

    <insert id="insertUser" parameterType="map">
        INSERT INTO
        `new_user`
        (`name`
        ,`yyyymmdd`
        ,`email`
        ,`introduce`)
        VALUES
        (
            #{name},
            #{birthday},
            #{email},
            #{introduce}
        )
    </insert>
</mapper>